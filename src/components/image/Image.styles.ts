import {styled} from '@/styles'

export const StyledImage = styled('img', {
  variants: {
    variant: {
      freelancerLogo: {
        transition: '$fast',
        height: '20px'
      }
    }
  }
})
